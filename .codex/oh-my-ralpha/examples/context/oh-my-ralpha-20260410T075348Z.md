# oh-my-ralpha Context Snapshot

- Task statement: Rebuild `oh-my-ralpha` as a new skill that fully inherits effective `oh-my-codex` Ralph behavior, then fuses the `.codex/oh-my-ralpha` truth-source workflow proven during the Python query-engine parity effort.
- Desired outcome: A new skill `oh-my-ralpha` that keeps Ralph's full useful execution skeleton, adds workboard/rounds-driven continuation, formalizes plan-first + sub-TODO decomposition + done-gates + team lanes, and is protected by routing/registry/contract tests.
- Known facts/evidence:
  - `oh-my-codex/skills/ralph/SKILL.md` is the behavioral baseline.
  - `oh-my-codex/src/cli/ralph.ts` provides runtime launch framing, append-instructions, and state bootstrap.
  - `oh-my-codex/src/hooks/keyword-detector.ts` seeds stateful modes and enforces execution gating.
  - Existing `oh-my-ralpha` currently has routing + registry + contract tests, but it does not yet keep Ralph full behavior.
  - Reviewer feedback established that the target must inherit Ralph first, not merely borrow selected ideas.
- Constraints:
  - New skill, not a rename of `ralph`
  - Use `.codex/oh-my-ralpha/working-model/context`, `.codex/oh-my-ralpha/working-model/state/*-todo.md`, `.codex/oh-my-ralpha/working-model/state/*-rounds.json` as core truth-source files
  - Keep implementation inside current workspace and the local `oh-my-codex` clone
  - Favor existing `oh-my-codex` features over inventing parallel mechanisms
- Unknowns/open questions:
  - How much of Ralph's PRD / visual gate / CLI launch surface should be preserved in the skill layer vs deferred to a later runtime mode
  - Whether `oh-my-ralpha` should be added to heavy-mode suppression/runtime seeding immediately
- Likely codebase touchpoints:
  - `oh-my-codex/skills/oh-my-ralpha/SKILL.md`
  - `oh-my-codex/src/hooks/keyword-registry.ts`
  - `oh-my-codex/src/hooks/keyword-detector.ts`
  - `oh-my-codex/src/hooks/__tests__/keyword-detector.test.ts`
  - `oh-my-codex/src/hooks/__tests__/pre-context-gate-skills.test.ts`
  - `oh-my-codex/src/cli/__tests__/oh-my-ralpha-contract.test.ts`
  - `oh-my-codex/src/hooks/__tests__/task-size-detector.test.ts`
