# working-model

This directory is the live writable truth-source root used by the `oh-my-ralpha` runtime inside this repository.

Runtime-generated data is written here:

- `context/`
- `state/`
- `plans/`
- `specs/`
- `logs/`

The repository also ships curated example artifacts under the sibling `examples/` directory.
